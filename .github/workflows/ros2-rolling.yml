name: ROS 2 Rolling CI

on:
  pull_request:
    branches:
      - rolling
  workflow_dispatch:

jobs:
  # build-and-test:
  #   runs-on: ubuntu-latest
  #   container:
  #     image: rostooling/setup-ros-docker:ubuntu-resolute-ros-rolling-ros-base-latest

  #   steps:
  #     - name: Build and test ROS packages
  #       uses: ros-tooling/action-ros-ci@v0.4
  #       with:
  #         package-name: |
  #           memfd_buffer_backend_msgs
  #           memfd_buffer
  #           memfd_buffer_backend_py
  #           memfd_buffer_backend
  #         target-ros2-distro: rolling
  #         no-symlink-install: true
  #         colcon-defaults: |
  #           {
  #             "build": {
  #               "cmake-args": [
  #                 "-DCMAKE_EXPORT_COMPILE_COMMANDS=ON"
  #               ]
  #             }
  #           }
  #         coverage-result: false

  windows-build-and-test:
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: src/memfd_buffer_backend

      - name: Download ROS 2 Lyrical
        shell: pwsh
        run: |
          $url = "https://github.com/ros2/ros2/releases/download/release-lyrical-20260807/ros2-lyrical-2026-08-07-windows-AMD64.zip"
          Invoke-WebRequest $url -OutFile lyrical.zip
          Expand-Archive lyrical.zip -DestinationPath C:\dev\lyrical

      - name: Set up pixi
        uses: prefix-dev/setup-pixi@v0.10.1
        with:
          manifest-path: C:\dev\lyrical\ros2-windows\pixi.toml

      - name: Build
        shell: cmd
        run: |
          call "%ProgramFiles%\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          call C:\dev\lyrical\ros2-windows\setup.bat
          pixi run --manifest-path C:\dev\lyrical\ros2-windows\pixi.toml colcon build --merge-install --cmake-args -DENABLE_LINT_TESTS=OFF

      - name: Diagnose test environment
        shell: cmd
        run: |
          call "%ProgramFiles%\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          call C:\dev\lyrical\ros2-windows\setup.bat
          call install\setup.bat
          where python
          where ament_cpplint
          pixi run --manifest-path C:\dev\lyrical\ros2-windows\pixi.toml where python
          pixi run --manifest-path C:\dev\lyrical\ros2-windows\pixi.toml where ament_cpplint
          C:\dev\lyrical\ros2-windows\Scripts\ament_cpplint.exe --help

      - name: Test
        shell: cmd
        run: |
          call "%ProgramFiles%\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          call C:\dev\lyrical\ros2-windows\setup.bat
          call install\setup.bat
          pixi run --manifest-path C:\dev\lyrical\ros2-windows\pixi.toml colcon test --merge-install || exit /b 1
          pixi run --manifest-path C:\dev\lyrical\ros2-windows\pixi.toml colcon test-result --verbose
